import Layout from "../components/Layout";

export default function Quiz() {
  return (
    <Layout active="quiz">
      <div>Quiz</div>
      <div>This is where you can access to some tests</div>
    </Layout>
  );
}
